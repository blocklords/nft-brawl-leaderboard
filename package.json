{
  "name": "nft-brawl-leaderboard",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ nft-brawl-leaderboard",
    "create-local": "graph create --node http://localhost:8020/ nft-brawl-leaderboard",
    "remove-local": "graph remove --node http://localhost:8020/ nft-brawl-leaderboard",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nft-brawl-leaderboard"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
